# Dark Pool Environment Variables

# WalletConnect Project ID (Required)
# Get yours free at: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Default Chain ID (Optional, defaults to 1 for Ethereum mainnet)
NEXT_PUBLIC_DEFAULT_CHAIN_ID=1

# DarkPoolRouter contract address
ROUTER_ADDRESS=0x0000000000000000000000000000000000000000

# RPC URL for on-chain commitment verification
RPC_URL=http://localhost:8545

# Chain ID for on-chain transactions (1=mainnet, 11155111=sepolia, 8453=base, 84532=baseSepolia, 137=polygon)
CHAIN_ID=1

# Engine wallet (signs on-chain txs: proveAndSettle, markFullySettled)
ENGINE_WALLET_KEY=0x...

# Yellow Network
YELLOW_WS_URL=wss://clearnet-sandbox.yellow.com/ws

# ZK Verifier contract address (deployed Groth16Verifier)
ZK_VERIFIER_ADDRESS=0x0000000000000000000000000000000000000000
